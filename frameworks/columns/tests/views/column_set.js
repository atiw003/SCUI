// ------------------------------------------------------------------------
// Tests for SCUI ColumnSetView
// ------------------------------------------------------------------------

module("SCUI.ColumnSetView",{
  
  setup: function() {
    
  },
  
  teardown: function() {
    
  }
  
});

/**
  A ColumnSetView Manages all of it's ColumnViews and upon being rendered
  it should draw one SCUI.ColumnView even if there happens to be no data.
*/
test("SCUI.ColumnSetView Should Render a single SCUI.ColumnView for the Root of the Tree", function() {
  ok(true);
});
